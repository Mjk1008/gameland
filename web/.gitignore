node_modules
.next
.env*
!.env.example
next-env.d.ts
.tsbuildinfo
*.log

# Liara installs from its own npm mirror; a lockfile pinned to registry.npmjs.org
# URLs hangs the Iran-side build. Resolve against the mirror instead.
package-lock.json
