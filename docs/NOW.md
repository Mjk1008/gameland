# NOW — handoff (2026-09-04)

اپ لایو است، پول واقعی. قبل از هر کار: `CLAUDE.md` و `docs/code-map.md`.

## کجا ایستاده‌ایم

- برنچ مبنا: **`mvp`**
- کامیت لایو: **`788d608`** — دیپلوی‌شده روی اپ `gameland` / [gamelandteam.ir](https://gamelandteam.ir)
- ریپو: `Mjk1008/gameland`
- `liara deploy` پوشهٔ کاری را آپلود می‌کند نه گیت. از چک‌اوت عقب‌افتاده دیپلوی نکن.

Rehearsal (`gameland-rehearsal`) همان روز سقف دیپلوی Basic خورد؛ **کد آنجا ممکن است قدیمی باشد**. دیتایش کلون پروداکشن بود. مبنا rehearsal نیست.

## امروز روی لایو آمد (مسابقه‌ها را بازنویسی نمی‌کند)

قرعه‌های موجود دست نخورده می‌مانند. `publishedGroups` اگر نباشد = از قبل عمومی.

| | |
|---|---|
| سهم جا‌مانده | یک سهم به رست؛ باقی در لیست بازماندگان |
| FC26 مقدماتی | متن قانون: تک‌بازی ۹ دقیقه؛ سقف استپر سید ۲ |
| شمارهٔ بازی | نمایش «بازی N» داخل هر براکت |
| سهم چهارم pending | ردیف approved می‌ماند؛ `settledAttempts` استخر قرعه |
| تهران + البرز | فقط **قرعهٔ جدید** یک گروه |
| انتشار | قرعهٔ **جدید** مخفی تا دکمهٔ انتشار |
| غیر تهران/البرز | در UI: «می‌ری تو براکت بازمانده‌ها»؛ ادمین از لیست بازماندگان به رست تهران یا `mixed:` اضافه می‌کند. **سید تهران خودکار نمی‌شود. استخر استان‌ها عوض نشده.** |

فایل‌های اصلی: `web/lib/bracket.ts`, `web/lib/store.ts`, `web/lib/iran-geo.ts`, `web/app/api/admin/publish-draw/route.ts`

## قانون قرعه

مسابقه‌های ذخیره‌شده را پاک/دوباره نچین مگر ادمین عمداً بزند. حساس است.

## کلاد

مک، سشن لیارا، و فایل کامیت‌نشده را ندارد. گیت‌هاب اگر وصل باشد پوش/PR می‌تواند. دیپلوی لیارا فقط با توکن در Secrets.

روی کلاد از **`mvp`** شروع کن.

## روی گیت نیست (دست نزن / قاطی نکن)

داخل `~/gameland` روی دیسک مک: WIP مچ‌سنتر (`web/app/center` و `lib/match-center*`). آن کپی یک کامیت از `origin/mvp` عقب است — از آنجا دیپلوی نکن.

## بعد از این فایل

`CLAUDE.md` → `docs/code-map.md` → فقط فایل مسیر تغییر.
